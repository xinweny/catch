<% if params[:query].present? %>
  <h2><%= @cats.length %> cats near <%= params[:query].capitalize %></h2>
<% else %>
  <h2>All cats (<%= @cats.length %>)</h2>
<% end %>
<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>
