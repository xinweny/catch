<% if params[:query].present? %>
  <h2><%= @cats.length %> cats near <%= params[:query].capitalize %></h2>
<% else %>
  <p>All cats (<%= @cats.length %>)</p>
<% end %>
<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>
