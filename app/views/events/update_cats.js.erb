function refreshUpdates(innerHTML) {
  const updates = document.querySelector('.updates-cats');
  updates.innerHTML = innerHTML;
}

refreshUpdates("<%= j render 'events/updates_cats', event: @event %>");
