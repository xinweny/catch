<% if params[:query].present? %>
    <div class="ml-5 py-4">
      <h3 style="color: #f5593d"><strong>Your results</strong></h3>
      <h3><%= @colonies.length %> colonies near <%= params[:query].capitalize %></h3>
    </div>
  <% else %>
    <h3>All colonies (<%= @colonies.length %>)</h3>
  <% end %>
  <div
    id="map"
    style="width: 100%;
    height: 800px;"
    data-markers="<%= @markers.to_json %>"
></div>
